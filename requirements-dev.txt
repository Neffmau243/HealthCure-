# Dependencias SOLO para desarrollo y tests (no van a producción)
# Instalar con: pip install -r requirements-dev.txt
#
# pytest        → correr los tests: python -m pytest app/tests -v
# httpx         → requerido por el TestClient de FastAPI (conftest.py)
# pytest-cov    → cobertura opcional: python -m pytest --cov=app app/tests

pytest>=8.0
httpx>=0.27
pytest-cov>=5.0
